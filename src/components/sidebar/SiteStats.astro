---
import SidebarCard from './SidebarCard.astro';
import { SITE } from '../../consts';
import { getAllPosts, getAllTags } from '../../utils/posts';

const [posts, tags] = await Promise.all([getAllPosts(), getAllTags()]);
const useBusuanzi = SITE.sidebar.busuanzi;
---

<SidebarCard title="站点">
  <ul class="stats">
    <li>
      <span class="label muted">文章</span>
      <span class="value">{posts.length}</span>
    </li>
    <li>
      <span class="label muted">标签</span>
      <span class="value">{tags.size}</span>
    </li>
    {useBusuanzi && (
      <>
        <li>
          <span class="label muted">浏览</span>
          <span class="value">
            <span id="busuanzi_value_site_pv" class="busuanzi-value">·</span>
            <span class="unit">次</span>
          </span>
        </li>
        <li>
          <span class="label muted">访客</span>
          <span class="value">
            <span id="busuanzi_value_site_uv" class="busuanzi-value">·</span>
            <span class="unit">人</span>
          </span>
        </li>
      </>
    )}
  </ul>
</SidebarCard>

{useBusuanzi && (
  <script
    is:inline
    async
    src="https://busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js"
  ></script>
)}

<style>
  .stats {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.4rem 0.75rem;
  }
  .stats li {
    display: flex;
    flex-direction: column;
    padding: 0.35rem 0;
  }
  .label {
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  .value {
    font-family: var(--font-mono);
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--text);
    margin-top: 0.1rem;
  }
  .busuanzi-value {
    display: inline-block;
    min-width: 1.5em;
  }
  .unit {
    font-size: 0.7rem;
    color: var(--text-subtle);
    font-weight: 400;
    margin-left: 0.1rem;
  }
</style>
